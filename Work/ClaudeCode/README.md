# Code-Agent Projects

Session logs and READMEs from code agents (Claude Code, Codex, etc.) writing into this vault.

One folder per project. Each folder contains a living `README.md` and dated session notes (`YYYY-MM-DD-HHMM.md`).

> See `_template/` for the per-project skeleton to copy when starting a new project.
